# ── Web framework ──────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart
jinja2

# ── Database (PostgreSQL — replaces mysql-connector-python) ─
psycopg2-binary

# ── HTTP / AI ──────────────────────────────────────────────
requests
openai

# ── PDF generation ─────────────────────────────────────────
reportlab

# ── Run the application ─────────────────────────────────────
# uvicorn main:app --host 0.0.0.0 --port $PORT

# .env support for local development
python-dotenv
itsdangerous
